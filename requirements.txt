# Core analysis
numpy>=1.24
scipy>=1.10
librosa>=0.10
soundfile>=0.12
scikit-learn>=1.3

# Web app
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Accounts, sessions, database
sqlalchemy>=2.0
authlib>=1.3           # Google OAuth / OpenID Connect
itsdangerous>=2.1      # signs the session cookie
bcrypt>=4.1            # password hashing
httpx>=0.27            # authlib's HTTP client
psycopg[binary]>=3.1   # Postgres driver (unused when DATABASE_URL is sqlite)

# Optional but strongly recommended
demucs>=4.0          # guitar stem separation (pulls in torch, ~2 GB)
yt-dlp>=2024.1.1     # analyse a song from a URL
